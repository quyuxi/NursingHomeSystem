<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="server.dao.SystemUserDao">
    <resultMap id="BaseResultMap" type="server.pojo.SystemUser">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="nursHomeId" jdbcType="INTEGER" property="nursHomeId"/>
        <result column="role" jdbcType="OTHER" property="role"/>
        <result column="realName" jdbcType="VARCHAR" property="realName"/>
        <result column="sex" jdbcType="VARCHAR" property="sex"/>
        <result column="birthday" jdbcType="VARCHAR" property="birthday"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
    </resultMap>
    <delete id="deleteSystemUserByUserName" parameterType="java.lang.String">

        DELETE
        FROM SystemUser
        WHERE id = #{id}
    </delete>
    <delete id="deleteSystemUserById">
        delete from SystemUser where id=#{id}
    </delete>


    <select id="findUserInfoById" resultType="server.pojo.SystemUser" parameterType="java.lang.String">

        SELECT *
        FROM SystemUser
        WHERE id = #{id}

    </select>

    <select id="queryRole" resultType="java.lang.String" parameterType="java.lang.String">
        SELECT role
        FROM SystemUser
        WHERE id = #{id}
    </select>

    <select id="findAllUsers" resultType="server.pojo.SystemUser">
        SELECT *
        FROM SystemUser
    </select>


    <insert id="createSystemUser" parameterType="server.pojo.SystemUser">

        insert into SystemUser (id, password,role,nursHomeId,realName,sex,birthday,phone)
        values (#{id}, #{password}, #{role},
                #{nursHomeId,jdbcType=VARCHAR}, #{realName,jdbcType=VARCHAR},#{sex},#{birthday},#{phone})

    </insert>

    <update id="updateSystemUser" parameterType="server.pojo.SystemUser">

        update SystemUser
        <set>

            <if test="role != null">
                `role` = #{role,jdbcType=OTHER},
            </if>
            <if test="realName != null">
                realName = #{realName,jdbcType=VARCHAR},
            </if>
            <if test="nursHomeId != null">
                nursHomeId = #{nursHomeId,jdbcType=VARCHAR},
            </if>
            <if test="birthday != null">
                birthday = #{birthday,jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                `password` = #{password,jdbcType=VARCHAR},
            </if>
            <if test="sex != null">
                `sex` = #{sex,jdbcType=VARCHAR},
            </if>
            <if test="phone != null">
                phone = #{phone,jdbcType=VARCHAR},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateUserPassword" parameterType="server.pojo.SystemUser">
        update SystemUser
        <set>

            <if test="password != null">
                `password` = #{password,jdbcType=VARCHAR},
            </if>

        </set>
        WHERE id = #{id}
    </update>

</mapper>