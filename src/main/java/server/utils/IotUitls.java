package server.utils;

import server.iot.pojo.Position;
import server.service.RingService;

import java.util.List;

public class IotUitls {


    public static Position[] getSafeArea(int elderId) {

//        RingService ringService = SpringUtils.getBean(RingService.class);
//        List<Position> positions = ringService.getSafeArea(elderId);
//        Position[] positionArr = new Position[positions.size()];
//        return positions.toArray(positionArr);
        return null;
    }
}
